<div class="max-w-md mx-auto">
  <h2 class="text-2xl font-bold mb-6">ログイン</h2>

  <!-- Googleログインボタン -->
  <div class="mb-6">
    <a href="/auth/google_oauth2" style="display: inline-block; padding: 10px 20px; border: 1px solid #ccc; text-decoration: none; background: #fff; width: 100%;">
      🔍 Googleでログイン
    </a>
  </div>

  <hr style="margin: 20px 0;">

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div style="margin-bottom: 15px;">
      <%= f.label :email, "メールアドレス" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", style: "width: 100%; padding: 8px;" %>
    </div>

    <div style="margin-bottom: 15px;">
      <%= f.label :password, "パスワード" %><br />
      <%= f.password_field :password, autocomplete: "current-password", style: "width: 100%; padding: 8px;" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div style="margin-bottom: 15px;">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, "ログイン状態を保持する" %>
      </div>
    <% end %>

    <div>
      <%= f.submit "ログイン", style: "padding: 10px 20px; background: #007cba; color: white; border: none;" %>
    </div>
  <% end %>

  <div style="margin-top: 20px;">
    <%= render "users/shared/links" %>
  </div>
</div>